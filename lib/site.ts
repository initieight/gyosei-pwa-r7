/**
 * サイト全体で使い回す運営情報・定型文。
 * トップページと /about で表現がずれないよう、文言はここに1本化する。
 */

/** 運営者名。実名にするかどうかは未決定のため、いまは編集部名義 */
export const OPERATOR_NAME = '行政六法 編集部';

/**
 * 連絡先メールアドレス。
 * 公開用のアドレスが決まったらここを差し替える。
 * 個人の常用アドレスをそのまま載せないこと。
 */
export const CONTACT_EMAIL = '';

/**
 * データの出典に関する定型文。
 * トップページ（app/page.tsx）と /about の両方でこれを使う。
 */
export const DATA_SOURCE_NOTE = [
  '条文本文は e-Gov 法令検索の法令データをもとにしています。',
  '出題実績は行政書士試験の過去問（R2〜R7）を独自に分析して根拠条文を割り当てたもので、公式の集計ではありません。',
  '集計方法上、取りこぼしや誤りが含まれる場合があります。最終確認は必ず一次情報（e-Gov・試験センター）で行ってください。',
] as const;

export const DATA_SOURCE_TEXT = DATA_SOURCE_NOTE.join('');
